#include <iostream>
using namespace std;

int main() {
	int t;
	cin >> t;
	int tans = 0;
	while(t--){
	    string s;
	    cin >> s;
	    if (s[0] == '+' || s[2] == '+'){
	        tans++;
	    }
	    else {
	        tans--;
	    }
	}
	cout << tans;
}
