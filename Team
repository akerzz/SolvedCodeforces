#include <iostream>
using namespace std;

int main() {
	int t;
	cin >> t;
	int tans = 0;
	while(t--){
	    int a, b, c;
	    cin >> a >> b >> c;
	    int ans = 0;
	    if (a > 0) ans++;
	    if (b > 0) ans++;
	    if (c > 0) ans++;
	    tans += (ans > 1 ? 1 : 0);
	}
	cout << tans;
}
